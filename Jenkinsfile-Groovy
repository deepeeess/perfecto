#!/usr/bin/env groovy
properties([gitLabConnection('deepeeess-gitlab')])
node {
  stage('\u2776 checkout') {
    checkout scm
  }

  stage("build") {
    gitlabCommitStatus("build") {
      // your build steps
    }
  }

  stage("test") {
    gitlabCommitStatus("test") {
      // your test steps
    }
  }
}
